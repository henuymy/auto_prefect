name: auto-notify
prefect-version: 3

deployments:
  - name: notify-single
    entrypoint: flows/notify_single_flow.py:auto_notify_flow
    parameters:
      config_path: config/tasks/example.json
    work_pool:
      name: default-agent-pool
      work_queue_name: default
