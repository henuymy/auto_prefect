name: auto-notify
prefect-version: "3.0"

deployments:
  - name: notify-daily
    entrypoint: flows/notify_single_flow.py:auto_notify_flow
    parameters:
      config_path: config/tasks/日通报.json
    work_pool:
      name: default-agent-pool
      work_queue_name: default
