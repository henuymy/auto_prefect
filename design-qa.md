# 运行监控中心 · 视觉验收

状态：实现范围已记录；需要在目标运行环境重新执行浏览器验收后，才能标记为通过。

实现保留了统一顶部工作栏、左侧运行时间线、中部运行记录和右侧可复用详情抽屉四个主要区域；没有加入侧边导航。该页面的业务范围已收敛为通报任务的最近 30 天运行概览。

- 桌面布局：时间线、运行记录和详情抽屉可以同屏工作；窄屏时详情位于记录区下方，筛选区自动换行，表格保留横向可读性。
- 时间范围与状态：标题、记录说明均为“最近 30 天”；记录和时间线只显示成功、运行中、失败。待执行只在“待执行（全局）”抽屉中展示，不再污染两类历史视图。
- 记录浏览：运行记录支持 10、20、50 条分页；时间线按日期分组，每天可以收起或展开，并提供分批加载更多。
- 状态汇总：成功、运行中、失败和待执行同置于“运行记录”标题栏。前 3 项打开状态汇总，待执行打开通报当前 Scheduled 队列，并显示下次执行时间和下一环节。
- 失败详情：默认业务概览优先显示失败步骤和脱敏业务摘要；技术详情才显示步骤消息、技术摘要和按级别筛选的日志。Prefect 暂不可用时保留已同步摘要并提示详情不可用。
- 可访问性：三种抽屉均使用对话框语义，打开后聚焦关闭按钮，支持 Escape 关闭和 Tab/Shift+Tab 焦点循环；关闭后恢复触发控件焦点。
- 新鲜度信息：页面 WebSocket 状态、上游事件时间、最近对账时间分别显示，避免把“页面已连接”误解为 Prefect 实时链路正常。

## 浏览器复验清单

- 在桌面与窄屏分别打开 `monitor.html`，确认文本不溢出、表格可读、抽屉不会覆盖关键操作。
- 用通报、Session Keeper 和驾驶舱三类样本验证：只有通报进入记录、时间线和待执行。
- 验证 Scheduled -> Running 后待执行减少，Failed/CRASHED 一直显示为失败而不是完成。
- 验证分页、时间线每日收起展开、筛选重置和失败详情降级。
- 用键盘验证三个抽屉的初始焦点、Escape、Tab 与 Shift+Tab；再确认关闭后焦点返回原按钮或记录。
- 连续制造至少 20 次通报状态变化，单独记录 Webhook 到页面可见的延迟，并计算 P95。

已知取舍：当前单 FastAPI 进程使用进程内 WebSocket Hub；页面连接成功不代表 Prefect Automation 和 REST 对账健康。监控前端独立端口尚未纳入 `scripts/run.ps1` 托管。
