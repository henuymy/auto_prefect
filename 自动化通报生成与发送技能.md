# Skill：自动化通报生成与发送（正式模板 + 临时副本 + JSON配置版）

## 一、技能名称
**自动化通报生成与发送技能**

---

## 二、技能目标
基于自动登录、报表下载、数据对比、Excel 模板联动、截图生成和微信群发送，实现通报流程自动化执行。

该技能采用：

- **正式模板 + 临时副本** 的运行模式
- **JSON 文件** 管理任务配置
- **多时间点调度 + 失败重试** 的执行机制

核心目标是：

- 避免重复通报
- 避免正式模板被异常流程污染
- 支持同一任务一天多次通报
- 支持执行失败后的延迟重试
- 保证只有发送成功后才更新历史基线

---

## 三、适用场景
适用于以下场景：

- 需要定时从业务系统下载报表
- 同一份报表一天需要通报多次
- 通报内容依赖固定 Excel 模板计算结果
- 需要将结果发送到微信群
- 需要避免报表未更新时重复通报
- 需要在某次执行失败时自动补偿重试

---

## 四、核心设计思想

## 1. 正式模板
正式模板用于保存**上一次成功通报后的数据状态**，是下一次执行时的数据基线。

正式模板中的内容通常包括：

- 原始数据页
- 公式页
- 展示页
- 通报词页
- 截图页
- 辅助页

其中原始数据页有两个作用：

- 为 Excel 公式提供数据源
- 保存上一次成功通报后的原始数据状态

正式模板不能在流程中途直接覆盖。

---

## 2. 临时副本
临时副本由正式模板复制而来，用于本次运行中的全部处理，包括：

- 写入本次新下载数据
- 联动模板公式
- 生成截图
- 提取通报词
- 承载本次发送前的中间结果

临时副本只服务于当前执行，不作为正式历史状态。

---

## 3. JSON 配置文件
任务不再通过 Excel 配置表管理，而是通过一个 JSON 文件统一定义。

JSON 配置文件负责描述：

- 报表下载参数
- 模板路径
- 临时目录
- 原始数据页名称
- 截图配置
- 通报词配置
- 目标微信群
- 每日多个执行时间
- 失败后的重试间隔与重试次数

---

## 4. 核心原则
整个技能遵循以下原则：

1. **先比对，后写入**
2. **先发送，后更新正式模板**
3. **发送失败，不更新正式模板**
4. **同一任务支持一天多次执行**
5. **某次执行失败后，等待一段时间再次重试**

---

## 五、输入与输出

## 输入
每个通报任务至少包括以下输入信息：

- 报表名称
- 登录方式
- 报表下载参数
- 正式模板路径
- 临时副本目录
- 原始数据页名称
- 截图页及截图区域
- 通报词页或通报词单元格区域
- 目标微信群
- 每日执行时间列表
- 失败重试间隔
- 最大重试次数

---

## 输出
每次任务执行后可产生以下结果：

- 本次下载的报表数据
- 临时副本 Excel 文件
- 截图文件
- 通报文字
- 执行日志
- 更新后的正式模板（仅发送成功后）
- 重试记录

---

## 六、JSON 配置设计

## 1. 配置结构示例
```json
{
  "tasks": [
    {
      "task_name": "日通报A",
      "enabled": true,
      "login_key": "system_a",
      "report_request": {
        "url": "https://example.com/report/download",
        "method": "POST",
        "params": {
          "report_id": "1001"
        }
      },
      "template_path": "/data/templates/report_a.xlsx",
      "temp_dir": "/data/temp/report_a/",
      "raw_sheet_name": "原始数据",
      "notify_sheet_name": "通报页",
      "notify_text_sheet_name": "通报词",
      "screenshot_ranges": [
        "A1:H20"
      ],
      "notify_text_cells": [
        "B2"
      ],
      "wechat_groups": [
        "A群"
      ],
      "schedule_times": [
        "09:00",
        "12:00",
        "18:00"
      ],
      "retry_interval_minutes": 10,
      "max_retry_count": 3
    }
  ]
}
```

---

## 2. 字段说明

### task_name
任务名称，用于日志和任务识别。

### enabled
是否启用该任务。

### login_key
登录配置标识，用于匹配系统登录逻辑。

### report_request
报表请求参数，包括：

- url
- method
- params

### template_path
正式模板路径。

### temp_dir
临时副本及中间文件保存目录。

### raw_sheet_name
原始数据页名称。

### notify_sheet_name
截图页或展示页名称。

### notify_text_sheet_name
通报词所在页名称。

### screenshot_ranges
截图区域列表。

### notify_text_cells
通报词所在单元格列表。

### wechat_groups
发送目标微信群列表。

### schedule_times
一天内的多个通报时间点。

### retry_interval_minutes
某次执行失败后的等待时间。

### max_retry_count
某次执行允许的最大重试次数。

---

## 七、执行流程

## 步骤 1：读取 JSON 配置
程序启动后读取 JSON 配置文件，加载所有启用的任务。

系统根据 `schedule_times` 判断当前是否到达某个任务的执行时间点。

---

## 步骤 2：触发任务执行
当系统时间命中某个任务的某个通报时间点时，触发一次执行。

一次执行对应的是：

- 某一个任务
- 某一天中的某一个通报时间点

例如：

- 日通报A - 09:00
- 日通报A - 12:00
- 日通报A - 18:00

这些都应视为独立执行实例。

---

## 步骤 3：自动登录目标系统
程序自动登录目标系统，获取有效的：

- cookie
- session
- token

若登录失败，则本次执行记为失败，进入重试流程。

---

## 步骤 4：下载本次最新报表
根据配置中的请求参数下载报表，得到本次新数据。

下载结果可以是：

- Excel
- CSV
- 接口结构化数据

若下载失败，则本次执行记为失败，进入重试流程。

---

## 步骤 5：读取正式模板中的旧数据
打开正式模板，读取原始数据页中的旧数据。

此时获得两份数据：

- 新数据：本次下载报表数据
- 旧数据：正式模板原始数据页中的数据

---

## 步骤 6：执行数据对比
将新数据与旧数据进行一致性校验。

对比必须发生在写入任何模板之前。

### 若数据一致
说明报表未更新，本次执行结束：

- 不生成临时副本
- 不写入模板
- 不截图
- 不发送通报
- 记录“数据未变化，跳过通报”日志

这类情况不属于失败，不需要重试。

### 若数据不一致
说明报表已更新，进入后续流程。

---

## 步骤 7：生成临时副本
以正式模板为基础复制一份临时 Excel 文件。

临时副本保留正式模板的全部结构，包括：

- 原始数据页
- 公式页
- 展示页
- 通报词页
- 截图页
- 辅助页
- 单元格格式
- 合并关系
- 公式关系

正式模板在此时仍保持不变。

---

## 步骤 8：将新数据写入临时副本
将新数据写入临时副本中的原始数据页。

写入后，其他依赖原始数据页的公式页、展示页和截图页自动联动更新。

这一步只发生在临时副本中，不影响正式模板。

---

## 步骤 9：生成通报内容
基于临时副本中的结果生成通报内容，包括：

- 截取指定区域图片
- 提取通报词
- 检查展示页结果是否正常

若截图失败、公式异常或通报词为空，则本次执行记为失败，进入重试流程。

---

## 步骤 10：发送微信群消息
按照既定顺序发送：

1. 先发送图片
2. 再发送文字

若发送成功，则进入正式模板更新流程。  
若发送失败，则本次执行记为失败，进入重试流程。

---

## 步骤 11：发送成功后更新正式模板
仅当微信群发送成功后，才允许更新正式模板。

更新方式为：

- 用临时副本覆盖正式模板
- 或将临时副本中的新数据和结果同步回正式模板

更新完成后，本次新数据成为下一次执行的旧数据基线。

---

## 步骤 12：清理临时文件并记录日志
任务结束后执行：

- 清理临时副本
- 清理截图文件
- 写入执行日志
- 记录发送结果
- 记录模板更新时间
- 记录重试情况

---

## 八、失败重试机制

## 1. 重试触发条件
以下情况属于执行失败，可进入重试：

- 登录失败
- 下载失败
- 数据解析失败
- 临时副本生成失败
- Excel 联动异常
- 截图失败
- 通报词提取失败
- 微信群发送失败
- 正式模板更新失败

---

## 2. 不需要重试的情况
以下情况不属于失败，不应重试：

- 数据对比结果一致
- 配置中任务被禁用
- 当前未到执行时间

其中“数据一致”说明报表未更新，本次自然跳过即可。

---

## 3. 重试逻辑
当某个时间点的任务执行失败后，不应直接放弃，而应：

- 等待 `retry_interval_minutes`
- 再次执行同一个时间点对应的任务
- 最多执行 `max_retry_count` 次重试

例如：

- 原计划执行时间：09:00
- 第一次执行失败
- 配置为重试间隔 10 分钟，最大重试 3 次

则可按如下方式执行：

- 09:00 首次执行
- 09:10 第1次重试
- 09:20 第2次重试
- 09:30 第3次重试

若仍失败，则将该时间点任务记为最终失败。

---

## 4. 重试期间的基线规则
重试期间必须保持以下规则：

- 正式模板不得更新
- 正式模板中的旧数据仍然作为基线
- 每次重试都重新执行“下载 → 对比 → 临时副本 → 发送”的完整流程

这样可以保证逻辑一致，不会因为上一次失败而污染状态。

---

## 5. 重试成功后的处理
若某次重试发送成功，则：

- 立即更新正式模板
- 记录该时间点执行成功
- 停止后续重试

---

## 九、状态流转规则

### 状态 1：待执行
等待到达某个配置时间点。

### 状态 2：执行中
任务已触发，正在执行。

### 状态 3：数据未变化
新旧数据一致，本次跳过，不重试。

### 状态 4：待重试
本次执行失败，等待下一次重试。

### 状态 5：发送成功
本次执行或某次重试已成功发送。

### 状态 6：基线更新成功
正式模板已成功更新。

### 状态 7：最终失败
已达到最大重试次数，仍未成功。

---

## 十、关键规则

## 1. 对比规则
对比对象为：

- 本次下载的新数据
- 正式模板原始数据页中的旧数据

必须先对比，再写入临时副本。

---

## 2. 模板更新规则
正式模板只允许在以下条件全部满足时更新：

- 数据发生变化
- 通报内容生成成功
- 微信群发送成功

否则正式模板不得更新。

---

## 3. 多时间点规则
同一个任务可以在一天中配置多个执行时间点。

每个时间点都是一次独立的执行实例，拥有自己的：

- 执行记录
- 失败状态
- 重试状态

---

## 4. 失败补偿规则
某个时间点执行失败后，应等待一段时间继续执行，而不是直接结束当天任务。

这样可以适应以下场景：

- 报表系统短时不可用
- 网络临时异常
- 微信发送接口瞬时失败
- 模板文件被占用

---

## 十一、异常处理

### 1. 登录失败
- 记录日志
- 进入重试

### 2. 下载失败
- 记录日志
- 进入重试

### 3. 数据解析失败
- 记录日志
- 进入重试

### 4. Excel 联动异常
- 记录日志
- 进入重试

### 5. 截图失败
- 记录日志
- 进入重试

### 6. 微信发送失败
- 记录日志
- 进入重试

### 7. 正式模板更新失败
- 记录日志
- 可视为失败并进入重试
- 若发送已成功但模板更新失败，需要重点记录，避免状态不一致

---

## 十二、技能总结
该技能的完整闭环为：

**读取 JSON 配置 → 命中时间点 → 自动登录 → 下载报表 → 读取正式模板旧数据 → 执行对比 → 不同则生成临时副本 → 写入新数据 → 模板联动 → 截图取词 → 发送微信群 → 成功后更新正式模板**

同时引入：

- **一天多次通报**
- **失败后延迟重试**
- **发送成功后再提交基线**

其核心思想是：

**正式模板保存历史基线，临时副本承载本次运行，JSON 管理任务配置，失败后按间隔继续补偿执行。**
