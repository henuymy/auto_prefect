__pycache__/
**/__pycache__/
*.py[cod]

# IDE settings
.vscode/
.idea/

# Python environments and build artifacts
.venv/
venv/
ENV/
env/
.worktrees/
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Node.js dependencies and build output
node_modules/
dist/

# Local credentials and session artifacts. These files are machine-specific
# and may contain account credentials, access tokens, cookies, or runtime state.
.claude/settings.local.json
legacy_modules/modules/*/config.json
legacy_modules/modules/*/runtime/
legacy_modules/modules/report-compare/*.xls
legacy_modules/modules/report-compare/*.xlsx

# Runtime output
/runtime/
logs/
temp/
cache/
.prefect-local/
*.log
~$*
.env
.env.*
# Local credentials, tokens, cookies, sessions, and keys
*.pem
*.key
*.p12
*.pfx
credentials.json
credentials*.json
secrets.json
secrets*.json
cookie*.json
**/cookie_dump.json
**/Cookies
**/Cookies-journal
tenxunwendang.txt
tenxunwendang*.local.*
*tencent*token*.txt
# Keep local JSON overrides out of Git: they contain environment-specific
# database, Prefect, webhook, or module credentials. Commit only templates.
config/tasks/local.json
config/tasks/*.local.json
config/modules/*.local.json
config/runtime.local.json
scripts/*.local.ps1
scripts/**/*.local.ps1
scripts/environment.local.ps1
/templates/
docs/prefect启动环境本地说明.md

# Local standalone Session lifetime experiment contains plaintext credentials
/session_lifetime_runner.py
/session_lifetime_results/
/.session-lifetime-runner-tests/

# Local FRP binaries and credentials
frp/frpc.exe
frp/frps.exe
frp/frpc.toml
frp/frps.toml
/frp.zip
/config/modules/modules.zip
/templates.zip
