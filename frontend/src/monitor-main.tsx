import React from "react";
import ReactDOM from "react-dom/client";
import { MonitorCenter } from "./monitor/MonitorCenter";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(<React.StrictMode><MonitorCenter /></React.StrictMode>);
